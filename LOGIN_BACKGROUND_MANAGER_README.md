# 登录页面背景管理功能

## 功能概述
登录页面背景管理功能允许管理员自定义登录页面的背景图片，提升用户体验和品牌形象。

## 主要特性

### 1. 背景图片上传
- 支持 JPG、PNG、GIF、WebP 格式
- 建议尺寸：1920x1080 或更大
- 自动创建上传目录
- 文件重命名避免冲突

### 2. 预设背景选项
- 默认渐变背景（原始样式）
- 蓝色渐变
- 紫色渐变
- 绿色渐变
- 橙色渐变

### 3. 背景管理
- 预览已上传的背景图片
- 设置当前使用的背景
- 删除不需要的背景图片
- 实时预览效果

### 4. 响应式设计
- 支持浅色和深色主题
- 移动端友好
- 现代化UI设计

## 文件结构

### 主要文件
- `public/login_background_manager.php` - 背景管理主页面
- `public/assets/css/background-manager.css` - 背景管理页面样式
- `public/login_roles.php` - 登录页面（已集成背景支持）

### 上传目录
- `public/uploads/backgrounds/` - 背景图片存储目录

## 使用方法

### 1. 访问背景管理页面
管理员登录后，访问 `login_background_manager.php` 页面。

### 2. 上传新背景
1. 点击"选择文件"按钮
2. 选择要上传的图片文件
3. 点击"上传背景图片"按钮
4. 系统自动处理并保存

### 3. 设置背景
- 点击预设背景选项直接设置
- 点击已上传图片的"设为当前背景"按钮
- 系统自动更新登录页面背景

### 4. 预览效果
点击"预览登录页面"按钮在新标签页中查看效果。

### 5. 删除背景
点击已上传图片的"删除"按钮，确认后删除文件。

## 技术实现

### 后端处理
- PHP 文件上传处理
- 会话管理背景设置
- 文件系统操作
- 安全验证

### 前端交互
- JavaScript 动态表单提交
- CSS 变量主题切换
- 响应式网格布局
- 悬停效果和动画

### 安全特性
- 文件类型验证
- 管理员权限检查
- 文件路径安全处理
- 会话状态管理

## 配置选项

### 支持的文件格式
```php
$allowed_types = ['image/jpeg', 'image/png', 'image/gif', 'image/webp'];
```

### 上传目录权限
```php
mkdir($upload_dir, 0755, true);
```

### 文件命名规则
```php
$filename = 'login_bg_' . time() . '_' . basename($file['name']);
```

## 样式定制

### CSS 变量
页面使用 CSS 变量系统，支持主题切换：
```css
:root {
  --bg-primary: #f5f5f5;
  --bg-secondary: #ffffff;
  --accent-color: #4a90e2;
  /* 更多变量... */
}
```

### 深色模式
完整的深色模式支持，包括：
- 背景色调整
- 文字颜色适配
- 卡片样式优化
- 侧边栏美化

## 注意事项

### 1. 文件大小限制
- 建议图片文件不超过 5MB
- 大文件可能影响页面加载速度

### 2. 图片质量
- 使用高质量图片获得最佳效果
- 避免过度压缩导致失真

### 3. 浏览器兼容性
- 现代浏览器完全支持
- 旧版浏览器可能显示默认背景

### 4. 性能考虑
- 背景图片会缓存到浏览器
- 首次加载可能需要一些时间

## 故障排除

### 常见问题

#### 1. 上传失败
- 检查文件格式是否正确
- 确认上传目录权限
- 查看服务器错误日志

#### 2. 背景不显示
- 检查文件路径是否正确
- 确认会话状态
- 清除浏览器缓存

#### 3. 样式异常
- 检查CSS文件是否正确加载
- 确认浏览器兼容性
- 查看控制台错误信息

### 调试方法
1. 检查浏览器开发者工具
2. 查看PHP错误日志
3. 验证文件权限设置
4. 测试不同浏览器

## 扩展功能

### 未来计划
1. **背景预设包** - 提供更多预设背景选项
2. **自动裁剪** - 智能裁剪图片适配不同屏幕
3. **背景动画** - 支持动态背景效果
4. **用户偏好** - 允许用户选择个人喜欢的背景
5. **批量管理** - 支持批量上传和管理

### 自定义开发
开发者可以扩展功能：
- 添加更多图片格式支持
- 实现图片压缩和优化
- 集成CDN加速
- 添加水印功能

## 总结

登录页面背景管理功能为ERPH系统提供了强大的视觉定制能力，让管理员可以根据需要个性化登录界面，提升用户体验和品牌形象。该功能设计合理、操作简单、扩展性强，是系统管理功能的重要组成部分。
