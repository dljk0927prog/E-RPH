# 课程与课本/作业关联功能说明

## 功能概述
实现课本/作业与课程的关联功能，让管理员在创建课本/作业时可以指定关联的课程，老师在创建教案时选择课程后，课本/作业选项会自动筛选出相关的内容。

## 数据库更改

### 新增字段
在 `subjects` 表中添加 `course_id` 字段：

```sql
-- 添加课程关联字段
ALTER TABLE subjects ADD COLUMN course_id INT NULL AFTER name;

-- 添加外键约束
ALTER TABLE subjects ADD CONSTRAINT fk_subjects_course 
FOREIGN KEY (course_id) REFERENCES courses(id) ON DELETE SET NULL;
```

### 数据结构
```sql
subjects 表结构：
- id: 主键
- name: 课本/作业名称
- course_id: 关联课程ID（可为空）
- is_active: 是否启用
- created_at: 创建时间
```

## 功能实现

### 1. 课本/作业管理页面 (`textbooks_homework.php`)

#### 🔧 **创建功能增强**
- **课程选择**: 添加"关联课程"下拉选择框（必选）
- **强制关联**: 必须选择关联课程才能创建课本/作业
- **智能布局**: 使用网格布局，名称、课程、状态在一行

#### 📊 **列表显示优化**
- **新增列**: 添加"关联课程"列显示课程名称
- **状态显示**: 缺少课程关联显示"⚠️ 缺少课程关联"
- **编辑功能**: 行内编辑支持修改课程关联（必选）

#### ✏️ **编辑功能升级**
- **行内编辑**: 支持同时编辑名称、课程关联和状态
- **网格布局**: 编辑表单使用网格布局，紧凑美观
- **数据保持**: 编辑时保持原有的课程关联选择

### 2. 教案管理页面 (`lessonplans.php`)

#### 🎯 **智能筛选**
- **动态筛选**: 选择课程后自动筛选相关的课本/作业
- **筛选逻辑**: 
  - 只显示关联到选定课程的课本/作业
  - 未选择课程时提示"请先选择关联课程"
- **实时更新**: 课程选择变化时立即更新课本/作业选项

#### 🔄 **状态保持**
- **选择保持**: 筛选时尽量保持当前已选择的课本/作业
- **智能清除**: 如果当前选择不在筛选结果中，自动清除选择

## 使用流程

### 管理员操作

#### 1. 创建课本/作业
```
1. 进入"课本/作业管理"页面
2. 输入课本/作业名称
3. 选择关联课程（必选）
4. 设置启用状态
5. 点击"创建课本/作业"
```

#### 2. 编辑课本/作业
```
1. 点击"管理"按钮进入管理模式
2. 点击要编辑项目的"编辑"按钮
3. 修改名称、课程关联或状态
4. 点击"保存"确认更改
```

### 老师操作

#### 1. 创建教案时的智能筛选
```
1. 进入"我的教案"页面
2. 选择"关联课程"
3. 课本/作业选项自动筛选为相关内容
4. 从筛选后的选项中选择合适的课本/作业
```

## 技术实现

### PHP后端逻辑

#### 数据库兼容性检查
```php
// 检查是否有course_id字段
$columns = $pdo->query("DESCRIBE subjects")->fetchAll(PDO::FETCH_COLUMN);
if (in_array('course_id', $columns)) {
    // 使用新的关联功能
} else {
    // 使用旧的兼容模式
}
```

#### 关联查询
```php
// 获取课本/作业及其关联课程
$subjects = $pdo->query('
    SELECT s.id, s.name, s.course_id, s.is_active, s.created_at, c.title as course_title 
    FROM subjects s 
    LEFT JOIN courses c ON s.course_id = c.id 
    ORDER BY s.created_at DESC
')->fetchAll();
```

### JavaScript前端交互

#### 动态筛选逻辑
```javascript
function filterSubjectsByCourse() {
    const selectedCourseId = courseSelect.value;
    
    let filteredSubjects = [];
    if (selectedCourseId) {
        // 只显示关联到该课程的课本/作业
        filteredSubjects = allSubjects.filter(s => 
            s.course_id == selectedCourseId
        );
    } else {
        // 不选择课程时不显示任何课本/作业
        filteredSubjects = [];
    }
    
    // 更新选项
    updateSubjectOptions(filteredSubjects);
}
```

## 数据关系

### 关联规则
1. **一对多关系**: 一个课程可以关联多个课本/作业
2. **强制关联**: 所有课本/作业必须关联到特定课程
3. **级联删除**: 删除课程时，关联的课本/作业的 `course_id` 设为 NULL

### 筛选逻辑
- **选择课程A**: 只显示关联到课程A的课本/作业
- **不选择课程**: 不显示任何课本/作业，提示"请先选择关联课程"
- **精准匹配**: 确保课本/作业与课程的精确对应关系

## 用户体验优化

### 1. 界面友好性
- **必选标识**: "关联课程 *"红色星号明确表示必选
- **状态显示**: "⚠️ 缺少课程关联"警告显示异常状态
- **智能布局**: 表单和表格布局紧凑美观

### 2. 操作便捷性
- **快速添加**: 快速添加按钮依然可用，但需要选择课程
- **实时筛选**: 选择课程后立即看到精确筛选结果
- **状态保持**: 筛选时尽量保持用户的选择

### 3. 数据一致性
- **外键约束**: 确保数据完整性
- **强制验证**: 前端和后端双重验证确保课程关联
- **安全删除**: 删除课程时安全处理关联数据

## 扩展建议

### 1. 批量操作
- 批量设置课本/作业的课程关联
- 批量创建某个课程的常用课本/作业

### 2. 统计分析
- 各课程关联的课本/作业数量统计
- 最常用的课本/作业排行

### 3. 模板功能
- 课程模板：预设某类课程的常用课本/作业
- 快速复制：从其他课程复制课本/作业配置

## 注意事项

1. **数据迁移**: 需要先运行SQL脚本添加 `course_id` 字段
2. **强制关联**: 所有新创建的课本/作业必须关联课程
3. **历史数据**: 旧数据可能存在无关联课程的情况，需要逐步修复
4. **性能**: 大量数据时注意JavaScript筛选的性能
5. **用户培训**: 需要告知用户必须选择课程才能创建课本/作业

## 更新说明 (2024年最新)

### 🔒 强制关联策略
- **创建限制**: 无法创建不关联课程的课本/作业
- **编辑限制**: 编辑时也必须选择课程关联
- **精准匹配**: 教案页面只显示与选定课程关联的课本/作业
- **数据清洁**: 确保所有课本/作业都有明确的课程归属

---

这个功能通过强制课程关联，确保了教学资源的精准分类和使用，避免了课本/作业与课程不匹配的问题，大大提升了教学管理的规范性和准确性。
