# 活动监控功能说明

## 概述

活动监控功能为ERPH系统提供了实时的用户活动跟踪和系统使用统计。管理员可以通过此功能监控系统使用情况，了解用户行为模式，并识别系统使用的高峰时段。

## 功能特性

### 1. 实时统计数据
- **总用户数**: 系统中注册的所有用户数量
- **活跃用户**: 今日有活动的用户数量
- **今日会话**: 今日用户登录会话总数
- **平均会话时长**: 用户平均在线时长

### 2. 24小时活动趋势图
- 显示每小时的系统访问量
- 自动识别高峰使用时段
- 可视化图表展示

### 3. 最近活动列表
- 实时显示用户活动记录
- 包含用户角色、操作类型、时间等信息
- 支持活动状态标识（成功、待处理、失败）

### 4. 自动数据刷新
- 每30秒自动刷新数据
- 手动刷新按钮
- 页面焦点管理（失去焦点时暂停刷新）

## 安装步骤

### 1. 运行数据库脚本
```bash
# 在MySQL中执行以下命令
mysql -u erph_user -p erph < add_activity_monitor_tables.sql
```

### 2. 文件结构
确保以下文件已正确放置：
```
public/
├── activity_monitor.php          # 主页面文件
├── inc/
│   └── activity_monitor_db.php  # 数据库操作类
└── ajax/
    └── activity_monitor_data.php # AJAX数据接口
```

### 3. 权限要求
- 只有管理员角色可以访问活动监控页面
- 确保数据库用户 `erph_user` 有相应表的读写权限

## 数据库表结构

### user_sessions (用户会话表)
- 记录用户登录会话信息
- 包含IP地址、用户代理、会话时长等

### user_activities (用户活动表)
- 记录用户的各种操作活动
- 支持活动类型、目标对象、状态等

### access_statistics (访问统计表)
- 按小时统计系统访问量
- 支持独立用户数、新用户数统计

## 使用方法

### 1. 访问页面
管理员登录后，在侧边栏点击"活动监控"即可访问。

### 2. 查看数据
页面会自动加载并显示：
- 统计数据卡片
- 24小时活动趋势图
- 最近活动列表

### 3. 刷新数据
- 点击"刷新数据"按钮手动刷新
- 系统每30秒自动刷新一次
- 页面失去焦点时会暂停自动刷新

## 自定义配置

### 1. 修改刷新间隔
在 `activity_monitor.php` 中修改：
```javascript
// 自动刷新数据（每30秒）
setInterval(() => {
    refreshData();
}, 30000); // 修改这里的数值（毫秒）
```

### 2. 添加新的活动类型
在 `ActivityMonitorDB` 类的 `getActionDescription` 方法中添加：
```php
private function getActionDescription($action) {
    $descriptions = [
        // ... 现有描述
        'new_action' => '新操作描述',
    ];
    return $descriptions[$action] ?? $action;
}
```

### 3. 修改统计查询
可以在 `getActivityStats` 方法中自定义统计逻辑，例如：
- 添加更多统计指标
- 修改时间范围（如改为7天统计）
- 添加用户角色分类统计

## 故障排除

### 1. 数据库连接失败
- 检查数据库配置是否正确
- 确认数据库服务是否运行
- 验证用户权限

### 2. 数据不显示
- 检查数据库表是否创建成功
- 查看浏览器控制台错误信息
- 确认AJAX请求是否正常

### 3. 自动刷新不工作
- 检查JavaScript是否启用
- 查看控制台是否有错误
- 确认页面焦点状态

## 性能优化建议

### 1. 数据库索引
确保以下字段有适当的索引：
- `user_activities.created_at`
- `user_sessions.login_time`
- `access_statistics.date, hour`

### 2. 查询优化
- 限制活动列表查询数量（默认10条）
- 使用分页处理大量数据
- 考虑缓存常用统计数据

### 3. 监控频率
- 根据实际需求调整自动刷新频率
- 避免过于频繁的数据库查询
- 考虑使用WebSocket实现实时推送

## 安全注意事项

### 1. 权限控制
- 只有管理员可以访问活动监控
- 记录所有监控页面的访问活动
- 避免记录敏感信息（如密码）

### 2. 数据保护
- 定期清理过期的活动记录
- 限制IP地址和用户代理信息的记录
- 遵守数据保护法规

### 3. 日志管理
- 监控异常访问模式
- 记录系统错误和异常
- 定期审查访问日志

## 扩展功能

### 1. 导出功能
可以添加数据导出功能，支持：
- CSV格式导出
- PDF报告生成
- 邮件定时发送

### 2. 告警系统
可以集成告警功能：
- 异常活动检测
- 系统负载监控
- 邮件/短信通知

### 3. 数据分析
可以添加更深入的分析功能：
- 用户行为分析
- 系统性能趋势
- 预测性分析

## 技术支持

如遇到问题，请检查：
1. 错误日志文件
2. 浏览器开发者工具
3. 数据库连接状态
4. 文件权限设置

更多帮助请参考系统文档或联系技术支持。
